import React from "react";

const CreatePost = () => {
  return (
    <div>
      <input type="text" placeholder="Post text..." />
      <input type="file" />
    </div>
  );
};

export default CreatePost;
